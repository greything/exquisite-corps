<h1>Posts#index</h1>

<% @posts.each do |p|%>

	<p>Id is: <%= p.id %>, belongs to chain <%= p.chain_id %>
		<br> 
		
		<%= link_to 'Destroy', p, :confirm => 'Are you sure?', :method => :delete %>		</p>

<% end %>
